# Playbook: reviewing a harness CLI update

**Use this when a harness CLI (Claude Code, Codex, or Antigravity `agy`) ships a new version and we need to know whether it affects Switchboard** — before or after upgrading. You may have no prior context on this project; this playbook tells you what to read, what to check, and what to produce. Keep the review evidence-based: **verify claims against our actual code, don't trust the changelog's framing.**

## Last reviewed

Per-harness snapshot of the last version vetted and the one-line verdict. A harness whose installed version is ahead of its row is unchecked. **Keep the verdict to one line:** durable facts about how the adapter behaves belong in `harness-behavior.md` (the single source of truth), and the blow-by-blow reasoning belongs in the review's **PR description** (git is where chronology lives). Each review also prepends a short entry to the **Review history** appendix at the bottom of this file — don't let these cells grow back into essays.

| Harness | Version | Date | Verdict |
|---|---|---|---|
| Codex | `0.153.2` | 2026-09-04 | No adapter-contract impact across five releases (189 commits): `exec/src/exec_events.rs` and `exec/src/cli.rs` are both byte-identical, and every rollout field we read survives. One new **`token_usage_record`** rollout record is now written by default (probe-verified @ 0.153.2) — the top-level dispatch's `_ => {}` arm skips it; its per-turn `turn_token_usage` is an unadopted, more-precise alternative to the `token_count` enrichment we read. **GPT-6-Astra** landed `visibility: hide`, so no picker change (same treatment as `gpt-daybreak-*`). Rollout compression dropped its reference-index/fork-pointer skips, widening what it would compress — but both `local_thread_store_compression` and `background_paginated_rollout_migration` are still `default_enabled: false`, so the plaintext-read risk stays latent. Live **14/14**. |
| Claude Code | `2.1.257` | 2026-09-01 | **Two breaks, both fixed** across two releases (2.1.252, 2.1.257). Break: **Fable 5.1 launched and moved the `fable` alias to `claude-fable-5-1`**, which the live-effort echo's exact-id allowlist did not list — the designed staleness tripwire fired (`live_claude_session_file_effort_matches_the_dispatched_level`), the new id was probed as recording effort verbatim, and it was added to `parser.rs::model_records_effort`. Second break, also fixed: **`permissions.blockReadsOutsideWorkingDirectories` (new, off by default) is bypass-immune** — the first Claude setting `--dangerously-skip-permissions` does not disable — and it silently defeated out-of-cwd attachment staging while the turn still reported success; the adapter now passes **`--add-dir /`** (**G33** closed; live-guarded with a negative control that proves the fence fires). Reviewing the fix also exposed that the in-cwd attachment control test had been staging outside the cwd since the store moved — fixed for all three harnesses. Everything else held: flags, `--effort` levels, stream vocabulary, `result` shape, and the cwd→`projects/` encoder are all unchanged; new session-file record types land on the skip arm. Also: **Fable's redaction is finally verified** (5.1 redacts, Opus-class — closes the credit-locked carry-forward since 2.1.197), and the model catalog can now be fetched server-side, though not in headless. Live **36/36**. |
| Antigravity (`agy`) | `1.1.26` | 2026-09-04 | No contract impact across six releases (re-probe + live **19/19**). The substance is the **Gemini 3.8 Flash launch**: probed dispatchable on subscription auth at all three efforts under the adapter's exact flags, so the picker's newest-Flash-only rule moves 3.7→3.8 (catalog-only change; durable record in `harness-behavior.md` §3.3/§3.4/§6). Three fixes land in our favour (`FD_CLOEXEC` on piped stdio, subcommands no longer hang on an inherited stdin, startup from a deleted cwd). **Open follow-up:** 1.1.24 made `agy` accept JSONC in `mcp_config.json` but our loader is strict `serde_json`, so a commented config shows zero MCP servers in the sidebar — display-only, not fixed. |

## 1. Get context first (read in this order)

1. **`AGENTS.md`** (repo root) — what Switchboard is, the crate layout, and the load-bearing rule: **the dispatcher/app are harness-agnostic; all harness-specific logic lives in the per-harness adapter** (`crates/harness/src/<harness>/`). A change "affects us" only where it touches an adapter's contract with the CLI.
2. **`docs/system-design.md` §9 (Harness integration)** — the adapter pattern and the capability matrix (what each harness exposes natively / derived / unavailable).
3. **`docs/harness-behavior.md`** — **the single source of truth** for how each harness behaves in the scenarios we depend on and how our adapter/frontend handles each. This is the spec the update is measured against. (Raw probe provenance is frozen under `docs/research/archive/<harness>-cli-observed.md`; cite it, don't edit it.)
4. **The adapter source for the harness under review**: `crates/harness/src/<harness>/` — at minimum `mod.rs` (spawn + args + outcome classification), `parser.rs` (event/stream parsing), `session_file.rs` (on-disk fields we read). `crates/harness/src/events.rs` defines the normalized event/`FailureKind`/`TurnOutcome` types every adapter maps into.

## 2. Get the update's changes

Prefer the changelog + commit diff over guessing. Per-harness sources:

| Harness | Repo / source | Get changes |
|---|---|---|
| Codex | `openai/codex`, tags `rust-vX.Y.Z` | `gh release view rust-v<new> --repo openai/codex --json body -q .body`; `gh api repos/openai/codex/compare/rust-v<old>...rust-v<new> -q '.commits[].commit.message'` |
| Claude Code | `anthropics/claude-code` (npm `@anthropic-ai/claude-code`) | `gh release view` / GitHub releases / npm changelog |
| Antigravity (`agy`) | Ships with the Antigravity desktop app and **auto-updates silently** — no GitHub repo, no release notes | **`agy changelog`** (added since this row said "no public changelog") prints per-version entries locally. Read it first, then **re-probe** (see §5) — the changelog is real but demonstrably incomplete. Always record the new version (`agy --version`); it may have moved since you last looked. |

If a code-level look is needed, fetch the specific PRs (`gh pr view <n> --repo <repo>`).

## 3. The dependency surface — what to check

For each changelog item, ask "does it touch one of these contracts?" Most items won't. The contracts Switchboard depends on (all per-harness; confirm specifics in `harness-behavior.md` + the adapter):

- **Invocation flags** — do the flags the adapter passes still exist and mean the same thing? (e.g. `--skip-git-repo-check`, `--dangerously-bypass-approvals-and-sandbox`, `-p`/`--print`, `--resume`/`--session-id`/`--conversation`, `--skip-trust`/`--yolo`/`--dangerously-skip-permissions`.) Renamed/removed flags are **breaking**. Grep the adapter's arg-builder.
- **Stream/event vocabulary** — did the JSON event `type`s or the fields we parse change? Map against `parser.rs`. *Additive* fields/events are usually safe **but verify the parser tolerates unknown types/fields** (most skip-by-default); *renamed/removed* event types or fields we read are breaking.
- **Turn-completion & error detection** — the terminal signal (`result` / `turn.completed`+`turn.failed` / `result.status` / transcript terminal record) and the error discriminants (`is_error`, `turn.failed`, status:error). Did the shape change?
- **Exit-code semantics** — especially the "exits 0 on SIGTERM / on error" cases the cancel path and outcome classification rely on.
- **Session-file layout & fields** — path/naming and the specific fields we read (rate limits, context window, model, usage/tokens, thinking). Map against `session_file.rs`.
- **Session-file record ordering invariants** — does the parser assume strict time-ordering of records, or does it tolerate out-of-order writes? Claude 2.1.150+ writes some `tool_result` records before their matching `tool_use` (within the same turn); our Claude parser tolerates this via a deferred-results queue. If a future Claude version delays a `tool_use` across a *turn boundary* the queue's per-session scope would no longer be safe. Re-verify after Claude updates by running `live_claude_tool_results_bind_after_restart` against a multi-tool prompt (`make test-live-claude`).
- **Auth-failure shape** — the strings/fields our detector matches.
- **Failure / quota message shapes** — substrings we classify on. (We prefer passing harness messages **verbatim**, which is drift-resilient; flag any place we substring-match.)
- **Resume command** — the interactive TUI-resume invocation we generate/copy (M4.8).
- **Config / registry sources** — MCP-server config path & schema, skills directories (read for the sidebar counts).
- **Model catalog** — new/retired models are not a CLI-contract change (the adapter forwards `-m`/`--model` and effort verbatim), but a model launch stales Switchboard's curated surfaces: the picker lists and defaults in `src/lib/agentSelection.ts`, the captured model/effort sets and defaults in `harness-behavior.md` §3.3/§3.4, and any README plan-gating note. On a launch: live-probe each new id's dispatchability on this account (the valid set is plan-gated and only discoverable via 400s), re-verify effort-level validity (it can be per-model and server-enforced; the Codex picker deliberately offers every level to every model and lets an invalid one fail the turn with the server's own enumeration, so a new model needs no picker change — but **Antigravity's `ANTIGRAVITY_MODEL_EFFORTS` in `agentSelection.ts` does** need one, since a mismatch there fails the dispatch), re-run any per-model behavior probes the harness has (Claude: thinking redaction), and consider repointing model-pinned live tests at the cheapest current-generation model.
- **Session-id assignment** — caller-controlled vs server-assigned (changes the capture/sidecar path).

## 4. Assess impact — verify against our code

For every changelog item that *might* touch the surface above, **confirm what we actually use** before calling it impacting. The changelog describes the CLI's whole behavior; we use a slice of it. Examples of the discipline:
- "Added `trace_id` to `turn.started`" → check `parser.rs`: if we **skip** that event, zero impact.
- "Removed legacy `--profile` plumbing" → grep the adapter: if we **never pass `--profile`**, no impact to our invocation (a user's stale config is their problem, surfaces as a normal harness error).
- "Changed usage-limit copy" → if we pass the message **verbatim** (not substring-classified), display is resilient; only a stale captured *string* in the docs.

Classify each relevant item: **breaking** (a contract we rely on changed) vs **additive/benign** (verify tolerance) vs **no-impact** (we don't use it).

## 5. Antigravity (`agy`) — review = re-probe

**`agy changelog` exists now — read it first, then re-probe anyway.** The "no changelog, so review = re-probe" premise this section was written on is obsolete: `agy changelog` prints per-version entries locally. But re-probing stays mandatory, and the 1.0.14 → 1.1.19 review is the proof. The single most damaging change it found — *a rejected tool call writes no transcript record at all*, silently losing it from reopened history — **appears in no changelog entry**. Changelogs describe what upstream chose to mention; probes find what actually broke.

Two further cautions this review earned the hard way. **`agy` auto-updates mid-review**: it moved 1.1.19 → 1.1.20 while the work was in flight, which invalidates every "verified @" stamp taken earlier. Re-run `agy --version` *at the end* and confirm it still matches what you stamped. And **read the changelog for the version you actually have**, not the one you started on.

**How to re-probe: run `make test-live-antigravity`.** The `live_antigravity_*` suite now covers the checklist this section used to spell out by hand — a normal turn still writing a parseable `transcript.jsonl` with a terminal `PLANNER_RESPONSE`, the stream's `init`/`result` shapes, resume, cancel, tool call/result pairing, dash- and slash-shaped prompts, and attachment reads inside and outside the cwd. Read the list of test names as the checklist; a green suite is the re-probe.

Three things the suite does **not** cover, so check them by hand:

- **The auth-failure line**, which can't be probed without logging out. It arrives on **stderr** as of agy 1.1.x (stdout is retained only as a backstop) — see `harness-behavior.md` §1.3. If a bump moves it back, the adapter's force-kill never fires and the turn hangs on agy's ~30s OAuth wait.
- **The quota / `RESOURCE_EXHAUSTED` message**, which can't be forced on demand. If the CLI-log line's shape moves, `scan_agy_log_for_error` goes quiet and a quota failure surfaces as a bare adapter error. Record it as a known gap rather than guessing the string (§7).
- **The model catalog** (`agy models`), which needs auth and network. A new generation stales `MODEL_OPTIONS.antigravity` and `ANTIGRAVITY_MODEL_EFFORTS` in `agentSelection.ts` (§3, "Model catalog").

Session-file and log paths *are* covered, indirectly but reliably: `live_antigravity_two_turns_hydrate_in_order` reads real history back off disk, and `live_antigravity_cli_log_names_the_conversation` asserts `--log-file` still carries the `Created conversation <uuid>` line the primary id-capture parses.

Captured shapes and the original probe commands are frozen in `research/archive/antigravity-cli-observed.md`. When probing by hand, always redirect stdin — `agy -p … < /dev/null` — because `agy -p` has no stdin timeout and hangs forever on an open one.

## 6. Record findings & verify

- **Report** the verdict (safe-to-update / breaking) with the specific impacted items and a recommendation.
- **Update the "Last reviewed" snapshot** at the top of this file: set the harness's row to the new version/date and a **one-line verdict**, and prepend a **one- or two-sentence entry to the Review history appendix** (bottom of this file). Keep both short — the snapshot is for scanning, not for the reasoning. Three homes for the detail, none of them a fat table cell: **durable adapter behavior → `harness-behavior.md`** (the single source of truth; don't restate it here), **the blow-by-blow reasoning → the review's PR description** (git owns chronology), and **open follow-ups → the verdict line** (so an action item isn't buried). Before condensing, make sure any durable fact you're dropping already lives in `harness-behavior.md`.
- **Record in `docs/harness-behavior.md` *only if the update actually impacts Switchboard*.** §6 "Version notes" is for things that change what Switchboard does or documents — a contract that changed, a captured shape/string now stale, a behavior we depend on that shifted (e.g. a model-version bump that flips a behavior we track, like thinking redaction). A **no-impact review gets NO §6 entry** — the "Last reviewed" snapshot + Review history line above are its complete record. Do not add "reviewed X, nothing relevant" commentary or per-update changelog summaries; that bloats the single-source-of-truth doc. Also update any now-stale captured shape or cell. **Do not edit the archived `*-cli-observed.md` files** (frozen provenance).
- **If a contract genuinely changed**, the fix goes in the **adapter** (never a `match harness {…}` in the dispatcher or `commands.rs`).
- **Run the live tests** for any adapter-touching change: `make test-live-<harness>` (claude / codex / antigravity). These spawn the real CLI and are the project's drift-detection mechanism — they need the developer's logged-in session and cost a little quota (see AGENTS.md "Live testing"). The fixture/`make test` path won't catch CLI drift.

## 7. If you need a live capture

Some shapes can't be read from a changelog (auth-failure output, quota messages, new event payloads). To capture: ask the developer to run the relevant CLI in the target state and share stdout/stderr/exit (e.g. a small headless turn with `--output-format stream-json` / `--json`). Quota/credit walls usually can't be forced on demand — record that as a known gap rather than guessing the string.

## Appendix: Review history

Append-only, newest first — **one or two sentences per review.** Point to `harness-behavior.md` for durable behavior and to the review's PR for the full reasoning; don't re-summarize the changelog here. (Reviews before 2026-07-01 carried their detail in the "Last reviewed" table cells; that prose lives in git history.)

- **Antigravity `1.1.20`→`1.1.26` (2026-09-04)** — no contract impact; the review's substance is the **Gemini 3.8 Flash launch** (1.1.25). `agy models` now heads its catalog with `gemini-3.8-flash-{high,medium,low}` and has dropped 3.5 Flash, and the base slug live-probed clean on a **subscription** account under the adapter's exact invocation — all three efforts dispatch, and `xhigh` is still refused client-side, pre-dispatch and quota-free with its own `(valid: low, medium, high)` enumeration. That makes the swap a catalog-only change (`agentSelection.ts` + `preferences.rs` defaults, 3.7 retired not added alongside), and it corrects the 1.1.25 changelog line, which claims the model appears only with a `GEMINI_API_KEY`. Re-probe found nothing else moved: stream `init`/`result` shapes, transcript hydration, resume, cancel, tool events, dash/slash-shaped prompts and out-of-cwd attachment reads all still behave, live **19/19**. Three upstream fixes land in our favour — `FD_CLOEXEC` on piped stdout/stderr (1.1.24; we pipe both), subcommands no longer hanging on an inherited stdin (1.1.23), and startup from a deleted working directory (1.1.24) — and 1.1.26's spurious customization-discovery log lines never matched our `--log-file` RPC-error scanner, which anchors on `rpc error: code = ` / `<STATUS> (code N)`. **One divergence found and left open:** 1.1.24 taught `agy` to parse JSONC in `mcp_config.json` (comments, trailing commas) while `antigravity/config.rs` uses strict `serde_json`, so a config `agy` reads fine renders as zero MCP servers in our sidebar — display-only, and Claude/Codex are unaffected. Note the suite is now latency-marginal: `live_antigravity_attachment_in_project_store_is_readable` timed out once at the shared 60s `LIVE_WAIT` under parallel load and passed in 16s alone, with a hand-reproduction of the same turn taking 36s of model time.
- **Codex `0.151.0`→`0.153.2` (2026-09-04)** — no adapter-contract impact across five releases (189 commits); `exec_events.rs` and `exec/src/cli.rs` are byte-identical between the tags, so neither the `--json` vocabulary nor the flags we pass can have moved, and a live probe re-confirmed the stream shape end to end. The one on-disk change is a **new `token_usage_record` rollout record**, persisted unconditionally by `rollout/src/policy.rs` and present in a fresh 0.153.2 rollout; the parser's top-level `_ => {}` arm skips it, and its `turn_token_usage` is a per-turn token source we do not adopt (`token_count.info.last_token_usage` enrichment is unchanged and still persisted in both history modes). Everything else on the surface is additive or out of the `codex exec` slice: `turn_context` gained `root_turn_id` while `turn_id`/`cwd`/`model`/`effort` are intact, `EventMsg` gained two transient `AuthRecovery*` variants, `rate_limits` is the shape §3 documents, the app-server `thread_history` rework is on a path we never read, and the planning tool going default-off (#41744) touches only `TurnItem::Plan`, which this parser has never consumed. Two items that looked risky and are not: **#41567's "resumed threads restore their saved working directory when none is supplied"** cannot reach us — `exec` always supplies `cwd` on `ThreadStart`/`ThreadResume`/`TurnStart` from its own `-C`/process cwd; and **rollout compression** lost its reference-index and fork-pointer skips (so it would now compress files it used to leave alone), but `local_thread_store_compression` and `background_paginated_rollout_migration` are both still `default_enabled: false`, leaving the "we read `rollout-*.jsonl` as plaintext" risk exactly as latent as prior reviews recorded it. **GPT-6-Astra** (#42605/#42632) is `visibility: hide` in `models.json` and OpenAI documents it as not shown in the picker, so it is not adopted — the `gpt-daybreak-*` precedent. MCP config and `.agents/skills` discovery are untouched (`skills/src/loading.rs` byte-identical). Live **14/14**.
- **Claude Code `2.1.251`→`2.1.257` (2026-09-01)** — two releases, and the review's substance is the **Fable 5.1 launch**: the `fable` alias moved to `claude-fable-5-1`, which the live effort echo's exact-id allowlist did not carry, so the tripwire that `model_records_effort` exists to trip did trip — the one code change here is adding the probed new id (durable record in `harness-behavior.md` §3.3/§3.4/§6), and the same probe finally settles Fable's thinking redaction after three versions of "credit-locked, unverified" (§3.2/§7.1). The second fix is **G33**: 2.1.257's new `permissions.blockReadsOutsideWorkingDirectories` is marked `bypassImmune` in the CLI's own permission table and, verified end-to-end, denies out-of-cwd reads under `--dangerously-skip-permissions` while the turn still ends `is_error:false` — which silently voided §3.7's out-of-cwd attachment staging for any user who opted in; the adapter now passes `--add-dir /` (root rather than the attachments directory, since users read across repos from Switchboard), with a live guard that seeds the setting at project scope. The rest of the dependency surface is clean (flags, effort levels, stream/`result` shapes, cwd encoder all byte-equivalent; new transcript record types skip-armed; the new stream-json line budget is on the stdin path we don't use), and the new fetched model catalog is explicitly not fetched on a headless launch. Live **36/36**, including the new fence guard.
- **Codex `0.149.0`→`0.151.0` (2026-08-30)** — no adapter-contract impact across 250 commits; the review's substance is two changes that stale documented behavior rather than break anything. **The paginated migration completed**: the store default flipped for durable threads (#40677, obsoleting the 0.148.0 entry's "default still reads `Legacy`" caveat) and the new `codex migrate-rollouts --apply` rewrites legacy rollouts in place — verified on an isolated `CODEX_HOME` copy, it flips `session_meta.history_mode` and converts the legacy message records, which G30's label-driven reader handles correctly. **And `ultra` stopped failing**: #41206 resolves an unsupported `ultra` client-side, so `gpt-5.5 + ultra` now completes (recording the *requested* level in `turn_context.effort`) while `gpt-5.5 + max` still fails with the server's verbatim enumeration. Follow-on decision (same branch): `gpt-5.5` was **retired from the picker** — OpenAI documents it as superseded and Terra as competitive at lower cost — which removed the only curated model either level could fail on and let `CODEX_MAX_ULTRA_MODELS` and its filter be deleted as dead code (§3.4). The changelog's riskiest-looking item, every model's `shell_type` moving to `unified_exec` (#39757/#39772), was probe-verified benign — both shell record generations survive with additive fields only. Also checked and clean: `exec_events.rs` byte-identical, additive `--thread-source` only, `AGENTS.md` still honored under our flags despite #39837, and the two new `gpt-daybreak-*` models are `visibility: hide` (not adopted). Live **13/13**.
- **Claude Code `2.1.241`→`2.1.251` (2026-08-30)** — no adapter-contract impact across six releases; the review's one finding is that **Opus 5 stopped failing `xhigh`/`max` effort when extended thinking is off and now clamps it to `high`** (catalog capability `thinking_disabled_effort_cap`, Opus-5-only, both disable routes, Sonnet 5 unaffected — probed @ 2.1.251), which removes a failure mode but makes G31's live effort echo disagree with the disk value for that configuration; durable record in `harness-behavior.md` §3.4/§6 and a README line, with no code change. Everything else on the dependency surface held — the cwd→`projects/` encoder was re-read out of the 2.1.251 binary (all four rules identical, `CLAUDE_CODE_PROJECT_DIR_NAME` still gated on `CLAUDE_CONFIG_DIR`), the alias table is unchanged with no new models, and Opus 5 re-probed as still redacting thinking. Live **34/34**.
- **Antigravity `1.0.14`→`1.1.20` (2026-08-25)** — the largest single-harness delta so far (35 releases). **Four breaking changes, all fixed**; the worst is upstream and unfixable — a rejected tool call now writes **no** transcript record, so reopened history loses it (mitigated as G32, README-visible, and absent from every changelog entry — the case for probing rather than trusting release notes). **Two capabilities adopted:** per-invocation `--model`/`--effort`, reversing harness-behavior.md §3.3's "no usable per-call model control — and we won't build one"; and `--output-format stream-json` as the adapter's control channel (content still comes from the transcript, which the stream lacks thinking text for). A third, `/usage` quota polling, was built to green tests and then **cut** — it costs 3.7–6.6s of phantom-busy time on every turn; see harness-behavior.md §5.1 for the payload and the reasoning. `agy` auto-updated 1.1.19 → 1.1.20 mid-review; 1.1.20's print-mode exit-code fix was re-probed and is benign (we never gated on exit codes, and the `state:"ERROR"` tool step G32 depends on is still emitted).
- **Claude Code `2.1.205`→`2.1.241` (2026-08-23)** — no adapter-contract impact across 34 releases; the review's substance was two changes that stale captured facts rather than break anything: the **`opus` alias moving to Claude Opus 5** (@ 2.1.219 — absorbed for free because our picker ships aliases, and Opus 5 re-probed as Opus-class on thinking redaction, 3/3) and **per-message `effort` appearing in the session file** (@ 2.1.212), which reverses §3.4's "not exposed" finding and opens a disk-only capability we do not yet read. Everything on the dependency surface held: the cwd→`projects/` encoding was re-read out of the 2.1.241 binary and is byte-for-byte the rule we implement (2.1.234's `CLAUDE_CODE_PROJECT_DIR_NAME` override only applies when `CLAUDE_CONFIG_DIR` is *also* set, and we set neither), the new `system/status` subtype and the additive `result`/`rate_limit_event`/`init` fields land on the parser's existing denylist/skip arms, the depth-agnostic `parent_tool_use_id` filter still covers subagents now that nesting defaults to depth 3, and print-mode SIGTERM→143 never reaches the G22 exit gate because the cancel path returns first. Durable facts in `harness-behavior.md` §3.3/§3.4/§6/§7.1 and **G31**; the PR owns the blow-by-blow. Live **33/33**.
- **Codex `0.146.0`→`0.149.0` (2026-08-22)** — **breaking**, and it is the follow-up the last two reviews flagged: `thread/start` now requests `ThreadHistoryMode::Paginated` (TUI 0.147.0, `exec` 0.148.0), and `rollout/src/policy.rs` persists `UserMessage` / `AgentMessage` / `PatchApplyEnd` / `McpToolCallEnd` only under `Legacy`, so prompts and answers hydrate empty and silently (`warnings: []`) while turn boundaries, usage, model/effort and the `hydration_key` survive. Everything else is clean — `exec_events.rs` byte-identical, live `--json` vocabulary probe-confirmed unchanged, `exec` flags intact, model ids identical; note the prior reviews' `ThreadStore::default_history_mode` check still reads `Legacy`, because the flip is at the *call sites*. Durable contract in `harness-behavior.md` §6/**G30** (closed — the fix landed the same day; live 13/13 post-fix, and the review incidentally hardened every forward path and added rename display; the PR owns the blow-by-blow).
- **Codex `0.144.3`→`0.146.0` (2026-08-01)** — no impact across 563 commits and two releases; the advertised work (paginated thread history, Agent Plugins/marketplaces, proxy-aware HTTP, realtime/audio, multi-agent v2, app-server perf) sits outside the `codex exec --json` slice. `exec/src/cli.rs` is byte-identical between tags and the `ThreadEvent` enum is unchanged — the only wire change is an additive `#[serde(default)]` `cache_write_input_tokens` on `Usage`/`TokenUsage`, which our `Value`-based parsers ignore. Rollout records gained fields we don't read (`session_meta.thread_source`/`context_window`, `task_started.started_at`, `turn_context.workspace_roots`/`permission_profile`/`multi_agent_version`, …) while every field we do read survived, and `patch_apply_end` still backs edit enrichment. #34777's turn-completion summary is an app-server v2 payload change (in `exec` it only feeds `--output-last-message`, emitting no extra stream events), `codex resume <id>` and `~/.codex/auth.json` are intact, and `models.json` is identical between tags so the picker/`CODEX_MAX_ULTRA_MODELS` need no update. Live **13/13**. **Follow-up:** the `history_mode` risk is unchanged — 0.145.0 shipped paginated history as experimental, but `ThreadStore::default_history_mode` still returns `Legacy` and a fresh 0.146.0 rollout confirms `"history_mode": "legacy"`.
- **Codex `0.144.1`→`0.144.3` (2026-07-13)** — the advertised Guardian rollback and advanced-reasoning TUI work do not touch `codex exec`, but live tests exposed an unadvertised rollout tool-record shift that broke shell-output hydration and edit enrichment; the Codex disk parser now supports both the legacy standalone calls and the new `exec` / array-output generation, which relies on the pre-existing `patch_apply_end` for structured edit content (durable contract in `harness-behavior.md` §3.1/§3.6/§6). Invocation flags, live stream vocabulary, model catalog, `turn_context`/`token_count`, and auth/exit semantics remain stable; `history_mode` is still `legacy`, and live **13/13** passes after the fix.
- **Codex `0.143.0`→`0.144.1` (2026-07-12)** — no adapter-contract impact; the review's substance was the **GPT-5.6 launch** (Sol/Terra/Luna + `max`/`ultra` efforts — durable facts in `harness-behavior.md` §3.3/§3.4/§6), which drove picker/catalog updates and repointed the model-pinning live tests to `gpt-5.6-luna` (live 12/12 @ 0.144.1). The 83-commit range's canonical-`TurnItem` migration preserves the `item.*` exec vocabulary and legacy rollout records via compatibility fanout (verified live incl. a 5.6 `command_execution` tool turn); #30488's reset-credit picker is TUI-only (`rate_limits` gained only an additive `credits` key). **Follow-up:** #30188 added `history_mode = "paginated"` rollouts that drop the legacy records we hydrate from — default is still `legacy` (verified in `ThreadHistoryMode`), but a future default flip would be breaking; also added this playbook's §3 "Model catalog" bullet so model launches are an explicit checklist item.
- **Codex `0.142.5`→`0.143.0` (2026-07-08)** — no impact. The 250-commit range is remote-plugins / app-server / Bedrock / system-proxy / sandbox work outside the `codex exec` slice: every flag we pass (`exec [resume] --json --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox -C`) still exists, and `turn_context.turn_id` + `token_count.{last,total}_token_usage` + `rate_limits` are intact (live **12/12**, including the `hydration_key` and rate-limit drift guards). The three items that look breaking are not: the `--permissions-profile`→`--permission-profile` rename (#30095) is on `codex sandbox`, not `exec`, and keeps a hidden alias; `SessionBudgetExceeded` (#29715/#29744) is an **app-server v2** wire value we never see; and the `AskForApproval::OnFailure` removal (#28418) drops a policy we never pass. #29835 adds a **new `world_state` rollout record** — observed in a fresh 0.143.0 rollout and skipped by the parser's unknown-type arm. Rollouts remain uncompressed plaintext, so the prior review's compression note stays latent. **Follow-up:** #29815 removed the auto-compaction opt-out; compaction was already default-on so nothing changes for us, but Codex's compaction record shape is still unprobed (`harness-behavior.md` §3.1) and can no longer be disabled to sidestep it.
- **Claude Code `2.1.197`→`2.1.205` (2026-07-08)** — no adapter-contract impact across eight point releases (mostly background-agent/daemon, TUI, and workflow fixes outside the `-p` headless slice we drive). The two items grazing our surface are both benign: 2.1.200 renamed the default permission mode to "Manual", which we bypass entirely with `--dangerously-skip-permissions`; and 2.1.201 stopped Sonnet 5 (the default model) using the mid-conversation system role for harness reminders, which is an **API-wire** change with no session-file footprint (verified against `~/.claude/projects`; the durable invariant is recorded in `harness-behavior.md` §3.1-provenance). The per-model thinking re-probe this doc mandates turned up a **standing documentation error unrelated to the bump**: Haiku 4.5 — a Switchboard-selectable model — streams un-redacted reasoning prose, contradicting §3.2/§7.1's "no default-selectable model returns reasoning prose" (both corrected). Opus 4.8 / Sonnet 5 confirmed still redacting @ 2.1.205, 3/3 each; Fable 5 credit-locked, unverified. 2.1.205's transcript-tampering auto-mode rule constrains the agent's own edits, not our external reads of `~/.claude/projects/*.jsonl`. No `--json-schema` / `--max-turns` / `--permission-mode` use in the adapter, so those fixes don't apply. Added `live_claude_haiku_thinking_is_not_redacted` — the first live coverage of the un-redacted `Thinking` path, which lost its guard when the `sonnet` alias moved to Sonnet 5 and the Sonnet test was inverted. Live **26/26**, including `live_claude_tool_results_bind_after_restart` (deferred-results queue still tolerates `tool_result`-before-`tool_use`).
- **Claude Code `2.1.172`→`2.1.197` (2026-07-01)** — no adapter-contract impact. Sonnet 5 became the default and `sonnet`→`claude-sonnet-5`, which **redacts extended thinking** (reversing Sonnet 4.6 — now no default-selectable Claude model exposes reasoning prose; `harness-behavior.md` §3.2/§6). Model-deprecation stderr warning, MCP `claude mcp` CLI changes, nested-skills loading, `--resume` fix, and stream-watchdog default were all benign or outside our surface. Live **12/12** after inverting the now-stale un-redacted-thinking test to `live_claude_sonnet_thinking_is_redacted`.
- **Antigravity `1.0.10`→`1.0.14` (2026-07-01)** — no contract impact (re-probe). Live 8/8 against `agy 1.0.14`: normal/dash-leading/adversarial turns, resume, tool events, and two-turn transcript hydration all intact; terminal `PLANNER_RESPONSE`, session paths, and `--print-timeout` default (5m) unchanged. `--help` has grown additive flags (`--model`, `--dangerously-skip-permissions`, `-c/--continue`, `--add-dir`, `--new-project`/`--project`) — the adapter already passes `--add-dir` + `--dangerously-skip-permissions` and deliberately selects no model. Corrected a stale doc claim: the prior review's "adapter never passes `--print-timeout`" follow-up was already fixed the same day by #40 (`--print-timeout 24h`); `harness-behavior.md` G21 flipped to Closed.
- **Codex `0.141.0`→`0.142.5` (2026-07-01)** — no impact. The `0.142` line is app-server / plugins / multi-agent-v2 / Bedrock / proxy work that doesn't touch the `codex exec` slice we use: invocation flags, stream vocabulary, and session-file records (`turn_context`, `event_msg` subtypes, `response_item`) are unchanged; `turn_id`-on-`ResponseItem`-metadata and env-context→world-state are additive/orthogonal. Cold-rollout compression is behind a disabled-by-default feature — a latent risk (we read `rollout-*.jsonl` as plaintext), not a current one. Live 7/7.
- **Antigravity `1.0.2`→`1.0.10` (2026-06-20)** — no contract impact (re-probe). Flagged a pre-existing gap: the adapter never passes `--print-timeout`, inheriting `agy`'s 5-minute whole-turn cap (still open).
- **Codex `0.139.0`→`0.141.0` (2026-06-18)** — no impact. `exec` flags, stream vocabulary, `turn_context`, session-file path, and auth/exit-code semantics unchanged; encrypted-secret CLI auth is opt-in and didn't displace `~/.codex/auth.json`.
- **Claude Code `2.1.170`→`2.1.172` (2026-06-11)** — no impact. Nested sub-agents (depth-agnostic `parent_tool_use_id` filter) and model-id `[1m]`/doubled-suffix fixes both benign.
